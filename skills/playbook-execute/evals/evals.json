{
  "skill_name": "playbook-execute",
  "evals": [
    {
      "id": 1,
      "prompt": "Run ./playbooks/implement-from-issue.yml",
      "expected_output": "The skill should parse and validate the playbook, topologically sort the 6 tasks, then execute them sequentially: fetch-issue → explore-codebase → approve-plan (human pause) → write-implementation → security-review → open-pr. On approve-plan, the skill must display the task prompt and wait for developer input before continuing. Each task should be reported with ▶ on start and ✅ on completion. The final summary must list all 6 tasks as completed.",
      "assertions": [
        "Skill reports validation success before starting execution",
        "Tasks execute in topological order: fetch-issue before explore-codebase; both before approve-plan; approve-plan before write-implementation; write-implementation before security-review; security-review before open-pr",
        "approve-plan triggers a human executor pause: separator printed, prompt displayed, developer response awaited",
        "Each task is reported with ▶ on start and ✅ on completion",
        "Final summary lists all 6 tasks as completed",
        "No tasks execute in parallel (Wave 1 sequential)"
      ],
      "files": [
        {
          "path": "./playbooks/implement-from-issue.yml",
          "description": "Standard implement-from-issue playbook from the PRD example"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "Run ./playbooks/test-on-failure.yml where the 'run-tests' task has on_failure: skip, and the 'deploy' task depends_on run-tests.",
      "expected_output": "When run-tests fails its success criteria, the skill should log a skip warning, mark run-tests as skipped, and also skip the deploy task (because its dependency was skipped). The final summary should show run-tests and deploy as skipped, not failed. Execution continues (does not stop) because on_failure was skip.",
      "assertions": [
        "run-tests failure triggers on_failure: skip behaviour (not stop)",
        "Warning message ⚠️ run-tests — skipped is shown",
        "deploy task is also skipped because its dependency (run-tests) was skipped",
        "Final summary lists run-tests and deploy under Skipped, not Failed",
        "Any tasks not depending on run-tests complete normally"
      ],
      "files": [
        {
          "path": "./playbooks/test-on-failure.yml",
          "description": "Playbook with run-tests (on_failure: skip) → deploy dependency chain"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "Run ./playbooks/cycle-test.yml where task-a depends_on task-b and task-b depends_on task-a.",
      "expected_output": "The skill should detect the cycle during validation (before any task executes), name the cycle (task-a → task-b → task-a), and stop with a clear error. No tasks should execute.",
      "assertions": [
        "Skill detects the cycle during validation, before starting execution",
        "Error message names both tasks in the cycle",
        "No tasks are executed",
        "Skill stops after reporting the cycle"
      ],
      "files": [
        {
          "path": "./playbooks/cycle-test.yml",
          "description": "Playbook with a circular dependency between task-a and task-b"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "Run ./playbooks/fallback-demo.yml where 'primary-impl' has on_failure: fallback:backup-impl, and primary-impl fails.",
      "expected_output": "When primary-impl fails its success criteria, the skill should skip primary-impl, insert backup-impl next in the execution queue, run backup-impl, and use its output for any downstream tasks that depend on primary-impl. The final summary should show primary-impl as failed/skipped and backup-impl as completed.",
      "assertions": [
        "primary-impl failure triggers on_failure: fallback:backup-impl",
        "backup-impl is executed immediately after primary-impl fails",
        "Downstream tasks that depend on primary-impl receive backup-impl's output",
        "Final summary shows primary-impl as skipped/failed and backup-impl as completed"
      ],
      "files": [
        {
          "path": "./playbooks/fallback-demo.yml",
          "description": "Playbook with primary-impl (on_failure: fallback:backup-impl) and a downstream task"
        }
      ]
    },
    {
      "id": 5,
      "prompt": "Run ./playbooks/retry-demo.yml where 'run-linter' has on_failure: retry and fails both the first and the second attempt.",
      "expected_output": "The skill should run run-linter once, verify success (fail), retry once, verify success again (fail), then treat as stop and halt the playbook. The final summary should show run-linter as failed.",
      "assertions": [
        "run-linter is executed exactly twice (original attempt + one retry)",
        "After both attempts fail, the skill applies stop behaviour",
        "Final summary shows run-linter as failed, not skipped",
        "No further tasks execute after the stop"
      ],
      "files": [
        {
          "path": "./playbooks/retry-demo.yml",
          "description": "Playbook with a single run-linter task (on_failure: retry) that fails both attempts"
        }
      ]
    },
    {
      "id": 6,
      "prompt": "Run ./playbooks/phase-mismatch.yml where task 'deploy' has phase: 'release' but the playbook only declares phases: [build, test].",
      "expected_output": "The skill should detect the invalid phase reference during validation (before any task executes), name the offending task and the undeclared phase, and stop with a clear error message.",
      "assertions": [
        "Validation catches the phase reference error before execution starts",
        "Error message names the offending task (deploy) and the undeclared phase (release)",
        "No tasks are executed",
        "Skill stops after reporting the validation failure"
      ],
      "files": [
        {
          "path": "./playbooks/phase-mismatch.yml",
          "description": "Playbook with phases [build, test] but a task referencing phase: release"
        }
      ]
    },
    {
      "id": 7,
      "prompt": "Run ./playbooks/report-with-vars.yml. The playbook has variables: { target_date: '', report_format: 'markdown' }. Two tasks: gather-data and format-report. The gather-data prompt contains {{ variables.target_date }} and format-report prompt contains {{ variables.report_format }}.",
      "expected_output": "Before starting execution, the skill should detect the variables block, present both variables to the developer (target_date as required since its default is empty, report_format as optional with default 'markdown'). After collecting target_date from the developer, the skill substitutes all {{ variables.* }} tokens in all prompt and input fields before any task executes. The tasks then run with the substituted values — no raw {{ variables.* }} tokens should reach any executor.",
      "assertions": [
        "Skill detects the variables block before execution starts",
        "Skill presents required variable (target_date) for collection and optional variable (report_format) with its default",
        "After collection, {{ variables.target_date }} is substituted with the developer-provided value in all fields",
        "{{ variables.report_format }} is substituted with 'markdown' (default) in all fields",
        "No raw {{ variables.* }} tokens appear in any task prompt or input sent to an executor"
      ],
      "files": [
        {
          "path": "./playbooks/report-with-vars.yml",
          "description": "Playbook with variables block, two tasks using {{ variables.target_date }} and {{ variables.report_format }}"
        }
      ]
    },
    {
      "id": 8,
      "prompt": "Run ./playbooks/include-demo.yml where the write-analysis task has prompt: '!include prompts/deep-analysis.md'. The file prompts/deep-analysis.md exists and contains detailed analysis instructions.",
      "expected_output": "Before validation, the skill should detect the !include directive in write-analysis task's prompt, read prompts/deep-analysis.md (path relative to the playbook file location), and replace the prompt value with the file's content. The executor then receives the full contents of deep-analysis.md as its prompt — not the !include token.",
      "assertions": [
        "Skill resolves !include before validation (Step 1.5)",
        "write-analysis task prompt is replaced with the full contents of prompts/deep-analysis.md",
        "The executor receives the file contents, not the literal '!include prompts/deep-analysis.md' string",
        "If prompts/deep-analysis.md did not exist, the skill should report a fatal error naming the task and missing file"
      ],
      "files": [
        {
          "path": "./playbooks/include-demo.yml",
          "description": "Playbook with a task using !include to reference an external prompt file"
        },
        {
          "path": "./prompts/deep-analysis.md",
          "description": "External prompt file referenced by the !include directive"
        }
      ]
    },
    {
      "id": 9,
      "prompt": "Run ./playbooks/parallel-tasks.yml where tasks fetch-a and fetch-b have no dependencies (both run first), then merge-results depends_on both fetch-a and fetch-b.",
      "expected_output": "The skill should compute the initial ready set as {fetch-a, fetch-b} and launch both as background agents simultaneously. While both run in parallel, progress shows both with '[parallel]' marker. After both complete successfully, merge-results becomes ready and runs alone. Final summary shows all 3 tasks completed.",
      "assertions": [
        "fetch-a and fetch-b are launched simultaneously in the first batch (not sequentially)",
        "Both fetch-a and fetch-b are reported with the [parallel] marker",
        "merge-results does not start until both fetch-a and fetch-b have completed",
        "Final summary shows all 3 tasks as completed",
        "Between the first and second batch, a batch-complete separator is printed"
      ],
      "files": [
        {
          "path": "./playbooks/parallel-tasks.yml",
          "description": "Playbook with fetch-a and fetch-b (independent) then merge-results (depends_on both)"
        }
      ]
    },
    {
      "id": 10,
      "prompt": "Run ./playbooks/parallel-stop.yml where tasks scan-a and scan-b run in parallel (no dependencies between them). scan-a fails with on_failure: stop. scan-b is still running when scan-a fails.",
      "expected_output": "When scan-a fails, the skill should NOT cancel scan-b. It should wait for scan-b to complete, then print the final summary and stop. The final summary shows scan-a as failed and scan-b as completed (or failed if it also failed). No further tasks are launched after the batch resolves.",
      "assertions": [
        "scan-a failure with on_failure:stop does not cancel scan-b mid-execution",
        "The skill waits for scan-b to complete before printing the final summary",
        "scan-a appears in the final summary as failed with its failure reason",
        "scan-b appears in the final summary with its actual outcome (completed or failed)",
        "No tasks that depend on either scan-a or scan-b are launched after the stop"
      ],
      "files": [
        {
          "path": "./playbooks/parallel-stop.yml",
          "description": "Playbook with scan-a and scan-b in parallel, scan-a failing with on_failure:stop"
        }
      ]
    }
  ]
}
